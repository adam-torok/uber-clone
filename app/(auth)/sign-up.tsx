import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const SignUp = () => {
  return (
    <SafeAreaView>
      <Text className={"p-5 pt-0"}>Sign Up</Text>
    </SafeAreaView>
  );
};

export default SignUp;
